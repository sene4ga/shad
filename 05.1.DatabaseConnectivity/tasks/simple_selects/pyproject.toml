[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "simple-selects"
version = "0.1.0"
dependencies = [
    "pypika",
]

[tool.setuptools]
py-modules = ["simple_selects"]